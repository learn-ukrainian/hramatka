# Hramatka agent entrypoint

Read [the canonical project instructions](agents_extensions/project-instructions.md)
before making changes. They define the repository boundary, task-scoped references,
verification, and completion gates. Maintain project guidance under
`agents_extensions/`; keep this entrypoint a short pointer.
