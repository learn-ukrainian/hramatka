"""Hramatka engine — local, gitignored prototype (slice 1).

See `.agent/tmp/hramatka/slice-1-build-plan.md` for the spec this package
implements. This directory is NOT part of the public repo git tree (see
`.gitignore`: `.agent/`) — Hramatka is a separate, private app; this is
local prototyping only, per repo governance.
"""
